# ConPath 统一外部验证：论文实验方法草案

**状态：2026-09-10，正式外部矩阵进行中，尚无充分训练后三个种子的完整结果。本文是既定实验方法的中文整理，不是结果报告；不增加实验、修改协议或授予论文主表资格。**

本轮检验的问题是：在相同部分地图和机器人足迹条件下，联合空间随机地图后验与可达性训练，是否比普通地图补全后计算连通性、独立单元随机补全和直接事件预测具有更好的可达性误差与校准。所有结论范围均为 **validation-only 开发验证**。

依据为[冻结协议](results/flatlands_external_formal_protocol_v1/protocol.json)、[数据审计](results/flatlands_external_formal_protocol_v1/data_eligible_v1/data_audit.json)、[执行说明](FLATLANDS_FORMAL_PROTOCOL_ZH.md)及协议绑定的模型、训练与评价器源码。协议 SHA-256 为 `409c8bc4a9142d75a62e6c3d67204ef7d5045ad38f74b45b5411f8bfe899c0e2`，冻结源码提交为 `a1fa04d10039731d27e723e58e1cf7b51da36aba`；最终论文须另引用实际检查点、完整预算和审计回执的哈希。

## 1. 数据、隔离与适用范围

使用 FlatLands 已发布的部分观测与参考 BEV；来源为3RScan、ARKitScenes、Matterport3D、ScanNet和ZInD。所有本轮源图均来自物理 `train` 归档，再按父级地点划分训练、校准和开发验证；发布数据的子场景名称不能代替父级地点隔离。

| 分区 | 父级地点 | 观测 | 固定端点查询 | 查询×三个半径 |
|---|---:|---:|---:|---:|
| 训练 | 985 | 1970 | 24572 | 73716 |
| 校准 | 97 | 97 | 1170 | 3510 |
| 验证 | 191 | 191 | 2460 | 7380 |

训练每个地点保留两个冻结视图，其他分区每个地点一个视图。一个验证观测没有合格查询，仍参加地图评分；事件评分覆盖190个父级地点。无查询观测不按参考标签删除，也不人为补造查询。

初始1000/100/192候选审计发现跨分区重复输入模板，随后在模型训练与推理之前，将涉及的19个父级地点、34张观测统一隔离，不补抽样本，原失败候选保留。相同输入不必然意味着同一建筑，也可能对应不同隐藏结构；这一排除会减少部分开放空间与歧义观测，限制了结论范围。通过版本的父级地点交集为零，并通过输入/参考支持区域的旋转、翻转重复审计。

**父级地点互斥不等于从未参与开发。当前191个验证父级地点中，有37个具有既往开发使用记录**；对应审计来源包括旧训练/验证地点、先前小规模试验和图库选择。本轮验证集还参与阶段收敛检查，因此不能称为全新确认性测试集，也不能据此证明未见建筑泛化。五个来源数量不等，汇总必须保留来源分层。

历史上曾读取物理test数据：旧训练/验证包含8/5条物理test观测，旧回放读取过其中5条验证观测；更早的512观测查询审计检查过53条物理test观测。这些访问集合不能简单相加。继续保留[读取范围更正](site/data/flatlands_read_scope_erratum.json)，撤回“物理test从未读取”的旧说法。本轮不新增读取物理test，`location_6`及最终测试在训练、选点、校准、绘图与报告阶段均保持锁定；现有开发结果不因此获得最终测试身份。

## 2. 共同输入、查询与预测含义

令 `S` 为发布的有效支持区域，`H = S ∩ unobserved` 为隐藏单元，`F = S ∩ observed_floor` 为已观测可通行单元，`B = S − (H ∪ F)` 为已观测障碍。外部方法输入为三通道 `[F,H,S]`；ConPath及内部对照使用的 `[F,B,H]` 是同一信息的无损表达。所有方法保留相同支持信息，不能把这项数据集提供的支持掩码当作自行预测的能力。

直接使用发布的部分观测和相机元数据，不重模拟遮挡、不按参考图改变mask、不追加输入通道。地图方法的预测必须将已观测证据投影回输入，并封闭支持区域外的所有单元；direct_query没有地图，这些地图约束不适用。约束违规计数在地图输出上如实检查，不在评分阶段偷偷修补。

起点是在读取参考标签前，按相机位置选择的最近已观测可通行单元。目标候选采用40/80/120格距离、0°至330°且间隔30°的输入侧极坐标清单，保留处于未知有效区域的合格单元。参考图中落在障碍上的目标仍作为自然负例保留。训练按输入哈希顺序取每观测最多8个查询，校准与验证使用全部冻结查询；不同方法不能换端点或只保留可达例子。

机器人半径固定为 **0、10、20个grid cell**，不换算成米。每张二值世界先按精确整数欧氏圆盘处理机器人足迹，再用四邻域判断起终点连通，地图边界外视作阻挡。绿色格子相连并不保证带半径机器人能通过；通道或端点放不下圆盘时，事件仍为不可达。

共同外部主预算为K=4，实际输出如下。

| 方法 | 每个外层种子的真实输出 | 原始事件分数 |
|---|---|---|
| ConPath coherent categorical | 一个模型，4张世界 | 逐世界精确可达指示值的平均 |
| ConPath original categorical | 一个模型，4张世界 | 同上 |
| Independent completion | 一个模型，4张独立单元采样世界 | 同上 |
| ConPath without reachability loss | 一个模型，4张世界 | 同上 |
| LaMa BEV adaptation | 4个独立训练成员，各1张世界 | 同上；四成员不是四个外层重复 |
| FM+XAttn literature reimplementation | 一个模型，4次真实生成 | 同上 |
| Deterministic completion control | 实际K=1，仅1张世界 | 单张世界的0/1可达结果 |
| Direct-query control | 地图输出不适用 | 网络sigmoid连续事件分数，没有世界票数 |

对地图方法，逐格可通行频率为 `m_i = (1/K) Σ_k W_ki`，事件频率为 `p_q = (1/K) Σ_k 1{Reach(W_k,q,r)}`，其中指示函数表示该世界是否存在可行通路。二者含义不同：**LaMa/FM的逐格四图比例不能叫路径概率**。K=4原始事件频率只可能为0、0.25、0.5、0.75或1；阈值0.8只接受4/4世界通过的查询。该离散结论不适用于直接查询的连续分数。确定性和直接查询对照分别标K=1/N/A，不复制地图凑成K=4。

## 3. 训练、检查点选择与充分性

所有方法在本轮共同数据上从头训练，外层种子固定为 **20260831、20260901、20260902**；LaMa每个外层种子的四个成员按协议记录的独立初始化种子训练，共需12个成员。旧K128、旧消融、PaSCo/S4C控制及短profile的检查点或成绩不能填入本轮矩阵。

| 方法组 | AdamW基础学习率 / weight decay | 有效batch / 微批×累积 |
|---|---|---|
| LaMa | 0.0001 / 0.01 | 8 / 8×1，每成员单独训练 |
| FM+XAttn | 0.0001 / 0.01 | 8 / 4×2 |
| ConPath与注册内部对照 | 0.0003 / 0.0001 | 8 / 2×4 |

LaMa固定为Big-LaMa的BEV适配，保留未知有效区域L1、hinge对抗项和多尺度判别器特征MSE，生成器权重分别为10、10、250；判别器使用独立hinge损失。不采用自然图像VGG/ResNet感知网络。FM固定既有U-Net与条件注意力、高斯初始化和未知有效区域速度MSE；采样使用Heun25步、CFG尺度2及各步约束投影。两者准确标为 **LaMa BEV adaptation** 和 **FM+XAttn literature reimplementation**，均不是作者官方checkpoint。

ConPath、原采样器和independent保留地图项、权重0.1的空间variogram项与权重2的可达性项；no_reach仅将事件训练权重设为0。确定性对照训练地图BCE，直接查询对照训练事件BCE，不能把协议中共享配置字段误写成所有方法实际采用了相同损失。ConPath类的训练事件前向值与精确连通性对齐，梯度通过既有可微路径传播。

训练按父级地点打乱采样，再均匀抽一个冻结视图。对于两个视图中仅有 `n` 个视图带查询的父级地点，抽到带查询视图时事件项乘 `2/n`，并按完整batch中具有任何合格查询的父级槽位归一化；抽到无查询视图时该槽位仍保留在分母。各微批保持完整batch权重，避免改变父级地点贡献。

完整训练之前，按冻结规则完成0步参照、1000步smoke、5000步pilot，并评估完整校准集与验证集。LaMa必须在全部四成员达到共同步数后做K4评估。阶段门槛包括有限loss、观测/支持违规为零、首末各100步主要训练loss均值至少下降2%，以及相对未训练参照的地图/事件Brier改善达到协议阈值0.0001。直接查询没有地图指标，对应地图门槛不适用。固定图片还须检查未知区几乎全可通行、几乎全障碍、斑块化和覆盖观测证据等退化；仅数值改善或图片成功导出不能代替视觉收敛审查。

通过pilot后，根据1000至5000步的校准地图与事件相对改善冻结10000或20000步完整预算：相关改善均低于2%时选10000，否则20000；直接查询只用可用的事件指标。前5000步保持基础学习率，之后cosine衰减至基础学习率的0.1。该规则不是“自动训练300000步”，达到预算上限也不等于充分收敛。

检查点统一以**校准集、父级地点等权的原始Event Brier**选择；与最小值相差不超过0.00001时，按hidden-map频率NLL、再按较早步数打破并列。直接查询的地图NLL不适用，并列按较早步数。完整阶段5000步之后每2500步形成校准候选；不在每个候选上反复用验证集选点。LaMa四成员必须共用一个选中步数，不分别择优拼成集成。

不同方法及种子允许选中不同步数；每行保留`selected_step`、`full_steps`和整次训练实际成本，不能将较早的合法最优检查点替换为较晚的较差检查点。所选检查点早于10000步仍按既定资格规则列诊断。正式资格还要求三个种子完成同一方法冻结的完整计划、最后三个校准检查点的地图/事件变化满足2%稳定门槛、loss与视觉退化检查通过，以及真实独立审计回执绑定数据、源码、训练和评估哈希。持续改善未停或仍有无法解释的退化时，保留“尚未充分训练/收敛”的状态。

## 4. 评分、权重、成本与图片

地图主指标在隐藏有效单元上计算真实二值世界逐格频率的Brier与NLL，同时报告已观测证据和支持范围违规计数。原生连续补全分数另列诊断，不能替换共同地图主指标；直接查询没有地图指标。NLL固定截断 `epsilon=0.000001`，避免有限K的0/1分数产生无穷值，所有方法使用同一规则。

任务指标为Event Brier、NLL、固定10个bin的ECE、阈值0.8的false-safe及coverage。False-safe定义为“被接受查询中实际不可达的加权比例”，并非所有负例中的误报率；coverage为接受查询的加权占比。无接受查询时false-safe未定义，保存为空而不是0。可靠性图与风险—覆盖率曲线保留全部相同分数的并列组，不按标签拆分并列制造优势。

主汇总按父级地点等权，并在父级内按其合格查询/半径事件平均；地图先在隐藏单元内平均，再按观测及父级地点平均。另报pooled汇总、reachable/unreachable、每个半径及半径×真值交叉分层。ECE由整体加权事件分箱计算，不是逐地点ECE的简单平均。每个来源分别报告相同指标，来源等权macro为次要汇总；某来源没有接受查询时，其风险不补零，需标有效来源数。三种子汇总保留各次结果；均值±种子标准差不是置信区间。

原始事件分数为主。另在校准集上拟合统一形式的单调、正则化Platt映射：每个方法/外层种子/所选检查点使用全来源、全半径的同一个映射，不按来源或半径另调参数；正则系数0.01。映射后的Brier/NLL/ECE与风险曲线单独标为诊断，不能称为保证校准，也不使用验证或测试标签拟合。

公平性指共同数据、信息、查询、足迹和评价规则，**不代表参数量、训练损失、完整步数或计算成本相等**。LaMa每个生成器50,966,465参数，四成员各有训练用判别器；FM为21,202,753参数。所有方法应同时报告成员数、总参数量、实际输出数、生成时间、完整更新时间、allocated/reserved峰值显存、真实批量forward次数、输入样本等效次数、速度求值NFE和训练总耗时。

FM固定Heun25对应每个生成样本50次速度求值；在本实现K4、双CFG分支下，每观测为50次批量forward、400个网络输入样本等效次数，不能将这三种计数混称。完整更新时间包含输入传输、生成及精确CPU可达性计算，排除磁盘和检查点加载；计时同步并包含首例冷执行。训练成本按所有进程会话回执累加，包含中断后的重放；选择较早检查点不抹去随后已消耗的训练时间。缺失会话结算时保留耗时下界标记。**共享GPU计时不能作为严格独占设备上的速度排名。**

图片沿用预先冻结清单：普通输入哈希案例、参考几何定义的瓶颈/不可达/多路径案例和多半径组，不按模型成绩重排或挑最佳世界。逐图标场景、查询、实际半径、方法、种子与validation-only；K4展示四张真实输出，确定性只展示一张，直接查询明确地图N/A。参考路径只能绘制已有精确几何见证，不能在S/G间添加无依据直线。训练损失图属于工程日志，不作为验证效果图。

## 5. 各比较能回答什么

| 注册比较 | 可支持的解释 | 不能单独支持的解释 |
|---|---|---|
| ConPath vs LaMa / FM | 相同信息与查询下，所注册完整方法的地图误差与可达性表现是否不同；地图质量改善是否伴随事件质量改善 | 外部论文官方复现、跨不兼容3D指标排名，或把差异只归因于空间相关性 |
| ConPath vs independent | 联合结构采样训练方案相对重新训练的独立单元方案是否有效 | **同边际、只改变相关性的因果证明**：该对照同时关闭global因素、将local核5改1、没有新copula，并重新训练；学习到的边际与优化过程也可能改变 |
| ConPath vs original | 固定候选中采样器及其与训练耦合机制的影响 | 同一检查点只替换推理随机数的纯后处理效应；两者均重新训练 |
| ConPath vs no_reach | 保留相同结构与共同选点规则时，加入事件训练损失的增益 | “完全未使用可达性信息”的比较：no_reach仍按共同校准Event Brier选点，并参加相同校准诊断 |
| ConPath vs deterministic | 所注册随机地图方案与实际K1确定性补全管线的任务表现差异 | 严格相同采样成本或只改变一个随机性因素的归因 |
| ConPath vs direct_query | 显式世界地图管线与直接事件网络的任务表现差异 | 只归因于是否输出地图；模型结构、监督和实际输出预算同时不同 |

正式结果全部待完成。现有未训练、1000/5000步、旧训练回放和历史队列数字只保留在明确标注的诊断部分，不复制进本轮正式主表，也不预写“ConPath胜出”。如果外部方法充分训练后更优，应如实记录，并分别检查地图与事件指标；如果消融无法支持空间结构或事件损失归因，应缩小研究主张。当前工作先完成已经注册的矩阵，不据短训练结果追加研究方向。
