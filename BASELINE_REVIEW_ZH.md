# 当前模型、公开论文成绩与改进方案

更新：2026-09-09。本轮完成现有检查点评估、公开成绩整理、建筑分组审计和训练集历史帧可行性检查；没有新增参数训练。读取范围检查发现旧开发划分混入物理test观测，下文已更正，不能称物理test从未触碰。

**目前不能声称 ConPath 优于其它论文。新发现的简单基线和建筑重叠也说明，原来的“室内效果好”需要缩小解释范围。此前将子场景 ID 隔离视为充分的数据隔离，检查不够完整；本轮纠正这一判断，数值和旧检查点原样保留。**

**读取范围更正：旧训练／验证包含原发布test中的8／5条观测，本轮回放也读取了这5条验证观测。更早的512观测查询审计检查过53条物理test观测（含32条ScanNet++）。因此撤回“物理test从未读取”的说法；旧 `test_evaluated=false` 等标志不能证明物理测试未触碰。详见[读取范围更正记录](site/data/flatlands_read_scope_erratum.json)。已停止进一步读取物理test图片，最终留出集需要审核全部历史访问并排除已检查的父级地点。**

## 1. 本轮实际完成的比较

随机方法统一取 **K=4 个完整世界**，确定性方法保留一个输出。ConPath 和独立模型各复用三次既有训练，取原 K=128 随机流的前四张，未按答案挑图。160个旧验证观测都计算地图指标，其中142个子场景有4,224个通路事件。新结果仍属于旧验证队列的诊断，不能成为新建筑泛化主表。

| 本地方法 | 输出数 | 通路概率 Brier ↓ | 接受最高分30%查询后的误判率 ↓ | 未知区域平均可通行 IoU ↑ |
|---|---:|---:|---:|---:|
| ConPath | 4 | 0.08007 ± 0.01251 | 4.89% ± 2.35% | 0.63607 ± 0.00699 |
| 独立单元随机模型 | 4 | 0.10219 ± 0.00781 | 4.94% ± 0.53% | 0.63486 ± 0.00819 |
| 小型确定性补全网络 | 1 | 0.08857 ± 0.00323 | 7.29% ± 0.68% | 0.68735 ± 0.00386 |
| 未知区域全部设为可通行 | 1 | **0.07237** | 13.43% | 0.63382 |
| 最近已观测单元填充 | 1 | 0.08894 | 10.16% | 0.59894 |
| 未知区域全部设为阻挡 | 1 | 0.46667 | 53.33% | 0.00000 |

“±”只表示三次训练之间的样本标准差；无训练的规则只有一个结果，不伪造三次重复。所有方法保留已观测证据和有效范围，使用同一圆盘尺寸、四邻接连通和查询标签；风险比较对分数相同的边界查询采用不看标签的等比例接受。原 K=128 的 ConPath Brier 为0.06749，不能与 K=4 行混称相同采样预算。

这张表揭示三个问题：

- 只和独立模型比较不充分。“全部可通行”规则的 Brier 点估计比 K=4 ConPath 更低，说明简单先验必须补齐。它的30%覆盖率误判率又明显更高，因此单看 Brier 也不够。
- 小型确定性网络的地图 IoU 更高，ConPath 的30%风险与独立模型接近；当前没有全面领先的证据。
- 对旧子场景进行2,000次配对重采样，“全可通行减 ConPath”的平均 Brier 差为 −0.00770，95%描述区间为[−0.02811, 0.01310]；K=128对应差为0.00488，区间[−0.01428, 0.02503]。均包含零，不能把上述点估计说成确定胜负。**这些区间仍按旧子场景分组，且检查点由旧验证集选出；发现建筑重叠后，更不能当成新建筑总体的置信区间。**

原始评估：`results/current_baseline_k4_v1/`；[公开统计 JSON](site/data/current_baseline_k4_analysis.json)。保存4,800张实际二值地图，12组共50,688条事件预测。六个随机模型的完整 K=128 事件回放及三个确定性模型事件回放，均与原CSV零差异。本轮执行时间包含额外128次回放及存档，**不是 K=4 部署时延**。

## 2. 数据划分出现了什么问题，怎样修正

旧训练160个观测，旧验证160个观测。虽然子场景 ID 不交叉，但不同房间、楼层、全景或重复扫描可能属于同一地点。

| 来源 | 上一级分组依据 | 与训练共享地点的旧验证观测 |
|---|---|---:|
| Matterport3D | 同一 house 下的 region | 19 / 32 |
| 3RScan | 官方3RScan.json的 reference 与 rescans | 5 / 32 |
| ZInD | 同一 home 下的楼层与 panorama | 2 / 32 |
| ScanNet | scene 中的 spaceId，合并 scanId | 1 / 32 |
| ARKitScenes | 官方 video_id → visit_id | 0 / 32 |
| 合计 | 来源名称也包含在分组键中 | **27 / 160（16.9%）** |

这证明旧划分不满足上一级地点隔离，**不直接量化它把分数抬高了多少**。后者必须通过从头训练、严格分组的控制实验测量。不能只删除这27条、沿用已经看过旧验证集的检查点，再宣称获得了干净留出成绩。

已生成新候选清单 `results/flatlands_parent_groups_v2/development_candidate.csv`：只引用物理 train 内的215,289个观测，按来源内的父级地点进行固定哈希排序，再约80%/10%/10%分给训练、校准、开发验证。所有房间和重复扫描跟随同一地点。划分得到：

| 候选分区 | 地点组数 | 观测数 |
|---|---:|---:|
| 训练 | 3,589 | 173,580 |
| 校准 | 445 | 20,224 |
| 开发验证 | 445 | 21,485 |

三分区地点交叉为零。53条 ARKitScenes 训练观测缺少官方 visit_id，单独隔离；不会把缺失 ID 当成独立建筑。v1曾保守隔离 ZInD 的地下楼层名称，v2正确识别负楼层，恢复19条观测；v1历史收据保留。

**候选清单不是已启动的大规模训练，也不是已通过全部门槛的正式数据集。** 尚需做输入/目标质量、盲选查询、跨来源重复、物理尺度和最终测试设计。原发布物理 test 与新候选训练仍共享2,886个地点组，所以原域内 test 不能直接承担“新地点”测试；本轮父级分组审计本身只读取元数据；但旧开发训练/验证和历史查询审计曾读取物理test图片，ScanNet++也不是全部未触碰。最终测试必须根据历史访问记录重新审核；当前不能保证整个原test目录符合未触碰留出条件。

来源规则可核对：[Matterport 数据组织](https://github.com/niessner/Matterport/blob/master/data_organization.md)、[ScanNet 数据组织](https://github.com/ScanNet/ScanNet)、[ZInD 数据组织](https://github.com/zillow/zind)、[ARKitScenes 数据说明](https://github.com/apple/ARKitScenes/blob/main/DATA.md)、[3RScan 官方说明](https://github.com/WaldJohannaU/3RScan)。对应下载地址、字节和SHA-256见[公开分组审计](site/data/flatlands_parent_group_audit.json)。3RScan映射使用官方服务器的元数据，不靠UUID前缀猜测地点。

## 3. 能不能直接引用其它论文的实验成绩

**可以引用公开成绩，优先复用；能否据此排出名次，取决于协议是否一致。** 本轮从 FlatLands v3 的表3/4整理了90条公开数值，保留表号、域内/域外、输出数、样本选择规则和原文所报标准差。见[参考CSV](site/data/published_baseline_reference.csv)和[完整协议说明](site/data/published_baseline_reference.json)。

例如，原论文表4的域内结果如下；它们是论文参考表，不与上一节本地队列混排：

| 原论文方法（K=4） | 遮罩能量分数 MES ↓ | 平均可通行 IoU ↑ |
|---|---:|---:|
| LaMa 集成 | 0.158 ± 0.139 | 0.713 ± 0.246 |
| 条件扩散 | 0.143 ± 0.152 | 0.763 ± 0.222 |
| 条件流匹配 | 0.143 ± 0.148 | 0.766 ± 0.226 |
| 流匹配＋交叉注意力 | 0.133 ± 0.138 | 0.766 ± 0.218 |

这里的 MES 是**地图联合样本评分**，不是通路概率 Brier，不能拿0.133与我们的0.06749比较。原文表3还按真实答案选择 K 张中最好的地图；它是带答案选择的上界统计，不是部署时可执行的选择策略。原文所报标准差也不能自行解释为三次训练波动。[FlatLands 论文原文](https://arxiv.org/html/2603.16016v3)

即使新增了同名 IoU/MES，目前也存在队列划分、训练规模、掩码、几何尺度和聚合方式差异。本地 K=4 ConPath 的平均地图 IoU 0.636、MES 0.239，与原论文 FM+XAttn 的0.766、0.133只能并列说明来源，不能据此推断谁更强或更弱。当前本地训练只有160份观测，而发布物理训练目录有215,342份。

本轮再次核查[官方发布仓库](https://github.com/1ssb/Flat_Lands)：固定提交6c4aabe54459d23d5161213a9e25e77f942545e8中，模型权重/训练工具仍列为待发布；已检查仓库与数据发布清单中未得到能重算通路指标的作者预测。论文均值不能反推出4,224个自定义通路事件。

因此对原计划作如下调整，**这是用户本轮“能直接用论文数据就不要复现”的最新偏好对旧计划的修订**：

1. 保留强外部对比、统一信息与预算、失败结果、独立训练重复及统计分析的要求；不是退回只做消融。
2. 先引用协议清楚的公开成绩；确需共同任务分数时，依次寻找作者逐样本预测、可兼容公开权重，再考虑最小必要的同协议适配训练。
3. 不再把重训 LaMa、流匹配、CogniPlan 全部列为无条件立即执行的任务。已完成接口和测速保留；112.8/285.2小时等只是原配方外推，没有启动这些长任务。
4. 我们新增的通路指标目前没有直接对应的论文分数。若论文仍主张该指标上的外部优势，就必须补至少一种可信强外部方法的共同评估；若作者产物始终不可用，必要的适配运行无法由引用不同任务的分数代替，或者需缩小论文主张。

## 4. 室外差，是结构、数据量还是模型架构

目前最明确的证据指向**观测和目标定义不一致**，不能仅归因于室外没有墙：

- 旧 UnScenes3D 模型 Brier 0.51142，独立模型0.51130，没有观察到优势。
- 由硬观测本身推出的验证误差下界已经是0.47574，约为实际误差的93%；只有15.66%的查询还可能通过未知区域补全改变。增加上下文不能推翻被最终硬投影锁死的错误。
- 当前目标只把 driveable 类当可通行，把同一XY列中的其他类别投影为阻挡；输入则由 LiDAR 几何射线与简化地面高度构造。几何空闲、语义可行驶、机器人真正可通行是不同定义；坡面、树冠、路缘、草地和高度净空都可能被二维压缩误判。
- 本轮在9个训练场景中固定各取最早4帧，共36帧。已观测且标签有效的单元冲突比例，按场景/帧等权为22.75%。307个被硬证据固定的假阴性中，207个直接存在被阻挡的查询端点。这个小样本诊断不能外推为全部478帧的精确比例。

室内通常有较稳定的布局规律，是合理假设，但当前室内输入由目标地图遮挡构造，室外输入由传感器推算；加上建筑重叠、数据规模和标签差异，不能把两组成绩差直接解释为墙体的因果作用。后续可在同一严格分组的室内队列加入预先固定的传感器噪声/遮挡扰动，同时在训练数据上对齐室外目标，检验输入质量影响。不要按验证结果挑选有利的噪声强度。

## 5. 历史关键帧与 Transformer：可以怎样做

历史帧能够补足遮挡和视野，时间注意力有成熟先例。[BEVFormer](https://arxiv.org/abs/2203.17270)通过历史BEV与当前查询交互，支持这种设计方向；但其检测/分割成绩不能保证改善我们的通路概率。[VoxFormer](https://openaccess.thecvf.com/content/CVPR2023/html/Li_VoxFormer_Sparse_Voxel_Transformer_for_Camera-Based_3D_Semantic_Scene_Completion_CVPR_2023_paper.html)说明稀疏几何与上下文结合的价值；它的时序实验还包含未来帧设置，不能照搬未来信息来评价在线规划。

本轮训练数据历史配准审计使用27对严格早于当前帧的点云：候选变换为 `inv(T_current) @ T_history`。0.3米邻近重合率均值从39.38%提高到45.59%，反向变换对照为19.61%；但中位距离只在11/27对改善。仅4/36帧能匹配本地独立原始 ego pose 文本，另32帧尚无这项独立验证。**这支持继续检查位姿，不能宣布时序融合已经提高预测精度。** 没有输入未来帧、密集累计完整地图或历史标签。

建议按以下顺序实施，时间窗口和选择规则只在训练/校准数据上确定：

1. **先改输入表达。** 用局部地面估计及2.5D高度、坡度、粗糙度、净空表达替代简单整列投影，明确机器人可通行定义。对真实传感器观测记录置信度或观测似然，允许不确定证据被推理修正；有效范围外仍封闭，高置信度真实障碍不能被无条件抹去。标签有效范围作为现有评估条件与部署可得范围分开报告。
2. **先做无需训练的因果融合。** 历史点云经相对位姿变到当前坐标，先检查静态区域配准、异常位姿和运动物体，再做置信度融合与时间衰减。关键帧按时间、位移、视角和新增覆盖选取，不按真实答案选。对齐失败时回退单帧。
3. **再测注意力。** 先采用较低分辨率BEV上的空间/时间注意力，与现有卷积解码和通路事件头衔接，记录特征缓存、位置/时间编码和全部前向成本。小样本下全量大Transformer更容易过拟合，不能直接假定一定更好。

正式机制实验使用一个2×2设计：

|  | 无注意力 | 有注意力 |
|---|---|---|
| 单帧 | 当前帧卷积基线 | 当前帧空间上下文注意力 |
| 历史帧 | 几何对齐＋卷积融合 | 相同历史窗口＋时间/空间注意力 |

时序两组接收同样帧和位姿，区别仅为融合结构。时间窗口预设1/2/4帧敏感性；四组训练数据、查询、目标和评估预算一致，分别做三次独立训练，另报告参数、训练成本和同延迟比较。这样能分别测量“信息变多”和“架构改变”，而不是只给新模型更多输入。以上为已记录的实验设计，尚未声称训练完成。

FlatLands附录S5报告的跨注意力消融变化很小：MES 0.095→0.096、最佳IoU 0.873→0.872；它提示注意力贡献取决于条件编码，不能作为必须上Transformer的证明。附录S4中CFG表明确标注为校准插值，不能当成实测消融证据。[原文附录](https://arxiv.org/html/2603.16016v3)

## 6. 训练数据少是否也是原因

是合理原因，但目前没有隔离这个因素的实验。室内原训练仅160个观测；室外478帧只来自9段训练场景、3个地点区域，相邻帧高度相关，“478帧”不等于478个独立环境。单纯增加同一场景的相邻帧不能证明覆盖了更多环境。

新协议应做两条嵌套数据规模曲线：一条增加独立地点数并固定每地点视角上限；另一条固定地点、增加观测/历史帧数。建议使用训练地点的25%/50%/100%嵌套子集，固定校准/验证地点、三种子和数据增强；主实验确保各模型充分训练，额外给出固定计算预算曲线，不能用相同epoch掩盖样本暴露与优化步数差异。若更大的地点集合持续改善、更多相邻帧收益很小，就支持环境多样性不足；若误差仍受硬观测下界控制，应继续修正输入/目标而不是延长训练。

下一轮可执行入口：先核清历史图片访问与最终留出资格，再对 `flatlands_parent_groups_v2` 候选完成训练侧质量与盲查询检查，核对尺度并固定正式开发规模；同时完成历史位姿质量判定和无需训练的观测融合小实验。只有新划分和观测协议通过后才从头训练，并将旧队列诊断、论文参考和新共同评估分别报告。

读取保护已新增到 `src/pathrel/data_access.py`：新开发协议必须提供父级分组和明确分区，物理test不能因provenance重新标注而解锁。215,289条新候选均通过此检查。旧数值独立复算的结果正确，但其原始收据中的读取范围标志有误，原文件与更正并存；旧图片回放入口现已拒绝再次读取。
