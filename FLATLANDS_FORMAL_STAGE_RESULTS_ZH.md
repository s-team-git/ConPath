# 外部基线阶段诊断：尚非正式三种子结果

更新时间：2026-09-10 04:05 UTC 左右。当前只有外层种子 `20260831` 的阶段证据；LaMa 每个阶段均使用四个真实独立成员。LaMa 正在 5000 步 pilot，FM 正在 1000 步 smoke。没有启动完整长训练。

以下数值来自同一冻结协议的完整校准/验证评估，并已用保存的世界地图做独立 CPU 重算。测试集与 `location_6` 继续锁定。所有结果都是开发验证诊断，不能用来声称论文主表、ConPath 优越性或充分收敛。

## LaMa BEV adaptation：1000 步后的实际变化

两列均为完整验证集，地图按 191 个父场景、事件按 190 个带查询父场景等权；2460 个查询，各评估 0/10/20 格半径。所有误差指标越低越好。

| 指标 | 未训练 | 1000 步，四成员 K=4 |
|---|---:|---:|
| 隐藏地图 Brier | 0.244704 | 0.126417 |
| 隐藏地图 NLL | 0.711070 | 0.887827 |
| Event Brier | 0.111117 | 0.072564 |
| Event NLL | 0.316027 | 0.623546 |
| Event ECE | 0.042963 | 0.065308 |
| 分数 ≥ 0.8 的误判率 | 0% | 13.1659% |
| 分数 ≥ 0.8 的覆盖率 | 0.0125% | 28.4562% |
| 已观测证据违反次数 | 0 | 0 |
| 支持范围违反次数 | 0 | 0 |

地图和事件 Brier 已改善，但 NLL、ECE 尚未改善，不能写成“全部指标更好”。未训练模型几乎不接受查询，因此其零误判率不能解释为更安全。K=4 原始世界事件分数达到 0.8，只可能是四张世界都判为可达；它不是经过验证的路径概率保证。

另一个需要在 pilot 检查的现象是：1000 步输出中，45.55% 的世界把未知区域至少 99% 判为可通行，而对应参考地图中只有 3.66% 达到这个比例。固定瓶颈案例也保留了没有补回参考障碍的输出。它们提示可通行区域估计偏大；短训练阶段还不足以断言是方法本身失败。

校准集也有相同方向的 Brier 改善：地图从 0.260509 降到 0.127782，事件从 0.110975 降到 0.071221。下一阶段必须继续检查全部成员、完整数据和固定图片，而不是只看这几个平均值。

独立重算通过：288 张校准/验证观测、1152 张实际世界、10890 个参考事件；地图、事件、分层和校准映射的最大数值差约 `1.62e-13`。恢复文件、四成员身份和 1000 步检查点同时核对。这是阶段正确性检查，不是三次重复的最终资格审计。

## FM+XAttn literature reimplementation：参照已经完成

未训练参照的完整验证指标为：隐藏地图 Brier 0.363284，Event Brier 0.308521，Event NLL 4.202812，Event ECE 0.310072。独立重算已通过。它们只用于判断 FM 自身后续训练是否改善，不能与 LaMa 的 1000 步拼成同阶段比较。

在本次并行运行环境中，未训练 FM 的四世界生成平均约 8.42 秒/观测；实际为 Heun 25 步、每样本 50 次速度求值、K=4 共 50 次批前向与 400 个单图等价输入。没有把 solver 步数、速度求值次数和实际批前向混为一项。

## 图片与接下来的门槛

本次已生成的报告快照为 `results/flatlands_external_formal_v1/reports/20260910T035037.828561Z/`，含 25 组验证图及 2 组训练日志图，每组保存 PNG/SVG/PDF。所有 27 张 PNG 已目视核对，81 个文件的 hash 已验证。训练日志图明确不是验证成绩，不进入 validation-only 网站。

当前验证图只放 LaMa 1000 步真实输出，其他方法明确留空。十个普通案例、瓶颈、不可达、多路径与多半径图均来自训练前冻结清单；没有按模型胜负挑选。多路径例中的两条参考路线可能彼此相邻，只证明内部格点不相交，不能解释为不同语义走廊或互不重叠的机器人足迹。

图片布局检查通过不等于模型退化门槛通过。必须完成 5000 步 pilot 的完整评估，核对 loss、地图和事件改善，再逐图检查三个阶段，才能按既定规则封存 10000 或 20000 步完整计划。当前没有方法获得论文主表资格。

ConPath、独立单元、去可达性损失和其他注册控制还需要在本轮相同数据上完成三种子矩阵；不会用旧 K=128 或旧小规模实验填补缺失结果。最终六个论文问题仍须等充分训练和共同审计后回答。
