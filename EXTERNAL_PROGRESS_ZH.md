# 外部比较：接口与成本检查

> **2026-09-10 论文收敛指令（覆盖下方历史计划）：** 外部模型训练/评估和自动续训已取消，保留全部结果；不扩数据、不新建模型方向、不再调参、不打开最终测试。已有原ConPath父级隔离三seed触发停止条件，当前仅整理论文及可审计图表。LaMa/FM只保留Related Work与欠收敛补充记录。当前计划见 [WORK_PLAN.md](WORK_PLAN.md)，证据见 [PAPER_EVIDENCE.md](PAPER_EVIDENCE.md)。

> **当前外部正式阶段（UTC 2026-09-10）：** 新协议、985/97/191父场景数据及18项固定图例已冻结，真实LaMa/FM中断恢复与258项回归通过；两个worker正在执行未训练/1000/5000阶段。三次充分训练与正式比较尚未完成。见 [本轮执行记录](FLATLANDS_FORMAL_PROTOCOL_ZH.md)。下文短训练和batch64测速保持历史诊断，不重复执行。

最新进展：LaMa和FM+XAttn的接入与有效批量64测速也已完成，见
[新阶段中文报告](FLATLANDS_EXTERNAL_PROGRESS_ZH.md)。以下保留CogniPlan阶段的原始记录与适用范围。

更新：2026-09-08。**CogniPlan 的官方生成模块已接通，完成32个固定训练观测的原生推理，
以及重建、对抗两个阶段各100次更新测速。正式长训练和外部主表尚未启动。**
本轮没有打开任何新的测试资产。六组已完成的内部消融不重跑、不覆盖。

## 1. 新数据划分已经固定

从 CogniPlan 补全器的原始训练资产中划分，不混用规划器中同名但内容不同的地图。
先对完整母地图及其8种旋转/镜像变换做几何哈希分组，再按每种布局的固定哈希顺序作80%/10%/10%划分。
3,000张地图没有发现上述变换后的重复几何；同一母地图的所有部分观测始终在同一组。
这是非官方的新划分，不能声称复现了原论文测试。

| 集合 | 完整母地图 | 部分观测 | 用途 |
|---|---:|---:|---|
| 训练 | 2,400 | 19,045 | 所有新训方法共用 |
| 校准 | 300 | 2,369 | 冻结模型后的同一校准流程，不参与模型训练 |
| 验证 | 300 | 2,381 | 预先约定的选模与诊断 |

每种布局分别800/100/100张母地图。固定测速样本来自32张不同训练母地图：房间11、隧道11、户外10；
母地图和观测都用哈希顺序选择，没有读取模型成绩后挑例。
划分文件在 `results/external_protocol_v1/`，母地图表哈希
`80c376ed08559c6f64115874f59b45d93c2f6f509d97441fe2e73b9f60b8378f`。
校准/验证查询、正式半径和各方法训练配方还需要冻结；划分完成不代表整套实验协议完成。

## 2. CogniPlan 接口检查结果

使用[官方源码固定版本](https://github.com/marmotlab/CogniPlan/tree/444fab8d5d3b8d2b004d83865088a9d707d2e3c8/mapinpaint)，
随项目保留 MIT 许可证和逐文件哈希。原生 Generator 有363,393个参数，Discriminator有183,361个参数。
发布权重为 `gen_00500000.pt`，SHA-256：
`dc8f5dff75637571fea70bae45115f020e7897d67d01b467789fc3feda6936a9`。

- 保留1/127/255部分地图编码、原生归一化和250→256边缘填充；输出中心裁剪回250。
- 推理固定使用均衡、房间、隧道、户外四组条件，不传入真实布局标签，也不按答案挑输出。
- 保留阈值−0.3、5×5闭运算后开运算、恢复已观测区域，最后封闭共同支持范围之外。
- 32个观测共128张预测全部有限；与官方原生后处理逐张精确一致；已观测区域零冲突。
- 对缺失某些灰度类别的边界输入，适配器直接使用已知语义，避免原版“前三个灰度值”推断失效；
  这项边界处理单独有测试，正常三状态输入仍要求与官方精确一致。
- 训练器与距离权重函数按原始定义加载，跳过非计算所需的可视化/日志依赖。
  重建、仅判别器、生成器+判别器三个更新分支与直接执行官方训练循环逐参数精确一致。

**这些数据来自公开权重的原始训练集，只能检查接口与基本输出行为。**
不能把其中新划出的校准/验证地图当作发布权重未见过的数据。正式新训练的模型才可使用上述留出划分。

首轮沿用官方cuDNN自动测速设置，跨进程回放时连续输出最多相差0.02389，800万预测单元中33个最终二值发生变化。
同进程适配器与官方入口仍完全相同，问题来自执行设置的可重复性。该v1保留为历史；
当前v2禁用自动测速和TF32，开启确定性算法，重新运行全部推理和两个阶段测速，并要求新进程精确回放。
网络、损失和权重未改。原先约6小时的估算由v2约12.45小时替代，不能混用两种执行设置的成本。
32例全部保留，视觉检查覆盖全部例子：观测丰富时结构较接近，稀疏观测下会猜错房间、孔洞与连接；
没有发现全图单色坍缩或已知证据被覆盖，但这不构成原论文精度复现或“基线质量已充分达标”的结论。

网站增加房间 `room_147`、隧道 `tunnel_265`、户外 `outdoor_56` 三个预先固定的首例，
每例含输入、四图可通行比例、完整参考和四种原生输出，共21张带中文标题/图例的真实图。
四图比例只能取0、0.25、0.5、0.75、1，未校准，也不是通路概率。

## 3. 实际测速与初步预算

RTX PRO 6000 Blackwell；PyTorch 2.12.1+cu130，FP32（禁用TF32），batch=24。
保留官方数据旋转增强、损失、优化器与更新顺序。固定32例先解码到CPU缓存；
每次从中抽24例，计入旋转/编码/传输与完整更新。每阶段先预热10次，再记录100次。
两个阶段分别从随机权重开始，仅用于测计算量，不假装经过原版10,000步重建预热或已经收敛。

| 阶段 | 平均每次更新 | 100次中的生成器/判别器更新 | PyTorch峰值分配显存 |
|---|---:|---:|---:|
| 重建 | 0.10500秒 | 100 / 0 | 2.46 GiB |
| 对抗 | 0.08929秒 | 20 / 100 | 2.44 GiB |

对抗阶段每5次才更新一次生成器，所以平均单次更快。重建阶段还原官方实现中保留的判别器/梯度惩罚前向计算；
没有为了美化测速删除这些计算。
按原版10,000次重建+490,000次对抗的完整配方外推，纯训练约**12.45小时/次、三次串行37.34小时**。
这是32例缓存和当前并发GPU负载下的估算，不是整个论文实验的预计完成时间；
还不含完整数据读取、验证、存档，也没有预先承诺正式配方必须机械照搬50万步。

另测了4张图生成、原生后处理和固定192个“端点/半径”事件的连通计算：平均3.46/1.16/7.71毫秒，合计12.33毫秒。
输入编码与主机到GPU传输未计入这张工程分解，GPU到CPU传输计入后处理。
端点不看答案，半径0/3/6格仅作为工程查询负载，尚未成为正式评价半径。
**GPU存在其他任务，因此这些数字不进入论文独占设备的速度比较表。**

完整逐次损失、时间、环境、原始图与哈希：`results/cogniplan_native_profile_v2/`。
本轮短训练已退出，没有启动50万步后台长训练。

## 4. FlatLands 尺度检查与另两类基线

160份固定训练元数据均为：512原图裁剪到256，窗口y=[192,448]、x=[128,384]，
相机移到(128,192)，resolution字段仍为0.01。
论文补充S2.4也描述这一裁剪，但S2.3又称512、0.01米/格经平均池化变为256、0.039米/格；
若只是两倍下采样且保持范围，应为0.02米/格，数值不能直接对齐。
本地记录支持“裁剪”这一解释，**尚不能独立证明物理标定正确**。
因此保留当前0/10/20格实验，不静默缩放，不据此声称特定真实车辆尺寸适用。
这不妨碍用相同栅格继续接入方法和测速；物理尺度结论仍受限。
依据：[FlatLands v3，S2.2–S2.4](https://arxiv.org/html/2603.16016v3)。

进一步读补充材料后，流匹配损失已经明确：S5给出
`x_t=(1-t)x_0+tF*`、目标速度`F*−x_0`，在未知有效区域用MSE；FM+XAttn共享这个损失，
额外在64×64、32×32注入条件注意力。主文笼统写BCE不能替代这里的方法专属定义。
S8.2/Table S12给出FP32、AdamW、学习率1e−4、权重衰减1e−2、cosine、有效batch64、30万步；
这些是文献配方，尚未作为本机正式训练配置冻结。

LaMa的BEV适配则禁用依赖自然图像通道的VGG/ResNet感知损失，保留重建/hinge对抗/多尺度特征匹配，
权重分别10/10/250。MapEx固定版本的LaMa子模块为
`castacks/lama@b61dcb33e063fe9586b50e2dc7b70f97d5046c1d`。
后续据此接入原始Fourier卷积骨干，不用小网络替代后仍称原生LaMa。
依据：[FlatLands v3，S5/S8.2](https://arxiv.org/html/2603.16016v3)、
[MapEx固定源码](https://github.com/castacks/MapEx/tree/53636bd1c79153acc3c74a532837d78c926bae5e)。

论文的Heun步数与NFE表口径也不能直接照抄：Heun通常每步需要两次速度求值，CFG另含有条件/无条件分支。
实现时将分别统计求解步数、速度求值、实际批量前向和样本等价前向，避免把“25步×4图”误写成完整计算量。

## 5. 紧接着执行

1. 接入上述LaMa骨干与条件流匹配，用已固定FlatLands训练样本检查接口、数值与成本。
2. 冻结CogniPlan原生数据的共同查询与评估器，并补充隐藏地图质量检查；原生权重只作为接口参考。
3. 依据完整规模、收敛诊断与实际资源确定各方法配方，启动共同划分下的正式三次训练。
4. 完成主比较、成本与不确定性分析后再冻结最终测试。没有新增正式外部优越性结论。

复现入口：

```bash
# 下列目录为新目录；脚本拒绝覆盖已有运行。
/home/hairo/miniconda3/bin/python3.13 scripts/prepare_external_protocol.py --output-dir results/external_protocol_replay
PYTHONPATH=src /home/hairo/miniconda3/bin/python3.13 scripts/profile_cogniplan_native.py --protocol-dir results/external_protocol_replay --output-dir results/cogniplan_native_profile_replay
PYTHONPATH=src /home/hairo/miniconda3/bin/python3.13 -m unittest discover -s tests -p test_cogniplan.py -v
```
