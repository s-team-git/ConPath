# FlatLands 正式外部比较：执行记录

本轮已于 UTC 2026-09-10 03:12 启动分阶段队列，尚未完成正式三次重复。源码提交 `a1fa04d`，协议提交与标签 `6694e86` / `flatlands-external-formal-protocol-v1` 已推送。258项回归通过。

本轮只验证“联合空间随机地图后验 + footprint-aware reachability”相对于地图补全、独立采样和直接事件预测的作用。LaMa 使用 **LaMa BEV adaptation**，FM 使用 **FM+XAttn literature reimplementation**；均从头训练，不是作者官方 checkpoint，不导入不兼容的论文 3D 指标。没有新增研究方向。

## 数据和测试锁定

初始代码为 `e951289`。当前环境为 Python 3.13.13、PyTorch 2.12.1+cu130、CUDA 13.0、RTX PRO 6000 Blackwell。正式协议记录冻结代码提交、全部源码/评价器 hash 和具体驱动信息。

候选按已确定的父场景分区、来源和 SHA256 顺序选取 1000/100/192 个训练/校准/验证父场景；训练每父场景两张观测，其他分区一张。扩大后的审计发现 19 张跨分区相同输入模板，其中 15 张也具有相同全自由参考地图。相同输入可以对应不同隐藏结构，不能直接断言是同建筑泄漏。为了采用严格输入去重协议，在任何模型训练/推理前统一隔离涉及的 19 个父场景、34 张观测，不补抽样本、不按模型表现选择。

原候选完整保留于 `results/flatlands_external_formal_protocol_v1/data/`，其失败状态没有改写。通过版本为 `data_eligible_v1/`：

| 分区 | 父场景 | 观测 | 查询 | 查询 × 半径事件 |
|---|---:|---:|---:|---:|
| 训练 | 985 | 1970 | 24572 | 73716 |
| 校准 | 97 | 97 | 1170 | 3510 |
| 验证 | 191 | 191 | 2460 | 7380 |

有一张验证观测没有有效查询，保留其地图评分，事件评分分母为 190 个父场景。所有图像来自物理训练归档，未新增读取任何物理测试或最终测试图；`location_6` 保持锁定。历史测试访问问题仍按既有更正记录披露，不能将这次开发验证称为从未观察过的最终测试。

隔离减少了部分开放空间/相同观测歧义案例，限制必须随结果报告。五个来源的数量不同，主汇总按父场景等权，另报 pooled、来源和半径分层，不能只看总平均。

## 共同条件

正式协议：`results/flatlands_external_formal_protocol_v1/protocol.json`。所有场景、输入、查询、支持范围和隐藏单元清单均用 hash 冻结。使用发布的部分观测，无额外历史帧。外部输入是 `[已观测可通行, 未知, 有效支持]`，ConPath 的 `[已观测可通行, 已观测障碍, 未知]` 是同一信息的无损转换。

起点从已观测自由区确定；目标由输入侧极坐标清单确定并在读取参考地图前落盘。保留参考图中被障碍覆盖的目标作为负例。半径固定为 **0/10/20 个栅格单元**，不转换为米。连通性采用四邻接和精确整数圆盘足迹，支持范围外封闭，已观测证据投影回输入。

外部主预算为真正的 **K=4**。LaMa 是四个独立训练成员，三个外部 seed 合计 12 个成员；成员不能作为三次独立重复。FM 和 ConPath 各生成四张地图。确定性/直接查询控制另标实际 K=1/N/A，不复制地图凑 K。逐格四图自由频率用于地图评分；事件必须先对每张世界地图分别计算可达性，再求四次指示值的平均。

外部 seed 固定为 `20260831 / 20260901 / 20260902`。内部控制沿用既有 ConPath 空间一致采样候选，同时预注册原版、去除 reachability loss、independent、deterministic 和 direct-query。它们在此次新数据上从头训练，旧 K128 和旧 PaSCo/S4C 结果保持历史用途，不重复运行旧实验。

## 恢复和阶段门槛

恢复工程测试已完成：完整默认 Big-LaMa 和 FlowUNet，256²、batch2，以真实 SIGINT 在优化器中途打断。跨进程恢复后，四个步骤的 loss、所有模型/优化器/调度器/RNG/采样器状态逐位一致。凭据为 `results/flatlands_external_resume_v1/verification.json`。这是恢复验证，不是收敛结果。

两视图中的无查询观测不会被删除；事件训练使用 `2/该父场景带查询的视图数` 权重，并以完整 batch 的有效父级槽位作分母，保证事件项按父场景等权且不因微批划分改变。

正式运行每个完整 update 写 `progress.jsonl`，每 100 步原子保存 `latest.pt`；安全暂停时在完整 update 边界立即保存，异常半步则回滚到最近完整状态并保留 `interrupted.pt`、归档多出的日志。非空目录禁止新运行覆盖，恢复必须核对配方。实际耗时按每进程独立回执累加，计入异常后的重放时间。

只允许两个并发 GPU 任务，项目预算约 60 GiB，每进程 allocator 上限 28 GiB。外部有效 batch8：LaMa micro8；FM micro4、累计2次。保留 FP32、确定性算法和固定已有损失，不按显存空闲机械增大 batch。

先完成未训练参照、1000-step smoke、5000-step pilot；每阶段在完整固定校准/验证集评估。LaMa 每阶段四个成员达到共同步数后评估真正 K4 集成。短训练只作为工程诊断。

通过 loss、地图、事件改善及证据约束检查，并完成固定图片退化检查之后，才另行冻结完整步数和 schedule。候选完整上限为 10000 或 20000 updates，由阶段收敛趋势决定；5000 步之后采用 cosine，末端学习率为初始的 0.1。没有启动 300000 步队列。达到预算但仍明显改善的模型不能标为充分收敛。

完整阶段按相同校准指标选择 checkpoint；LaMa 四成员共用一个选择步数。最终三次重复及统一审计完成前，不能进入正式优越性结论。

## 产物与解释

当前阶段状态读取 `results/flatlands_external_formal_v1/stage_status/`。汇总目录为 `results/flatlands_external_formal_v1/`，包含 `summary.csv`、`metrics.json`、`report.md`、`figures/` 和 `reproducibility.json`。缺失值保持缺失，报告区分阶段诊断和充分训练后的正式比较资格。

图片从输入盲选清单和预冻结参考几何类别生成，所有方法展示同一场景/查询/半径，标注 seed 和 validation-only。两条不相交栅格路径仅是几何见证，不冒充不同语义走廊；窄瓶颈检查同时要求端点容纳对应足迹。

只有充分训练、三个独立外部种子、共同验证和测试锁定审计都通过后，才回答 ConPath 是否优于外部补全、优势是否来自空间相关性和可达损失。外部方法胜出或阶段失败都如实记录；没有完成的项目不写成论文结果。
