# 父级地点隔离的小规模验证 v1

2026-09-09。用户已授权在扩展效果图后连续执行基线验证，并根据结果继续改进模型。

这是一轮开发实验，不是最终测试，也不是对其它论文公开分数的直接复现。旧队列结果独立保留。

## 固定数据与查询

只读取 FlatLands 发布包中物理 `train/` 的数据；旧 provenance 字段保留为来源元数据。
使用 v2 父级分组候选，在五个室内来源分别按哈希选取训练20、选优5、验证8个地点，
每地点固定一个观测，总计100/25/40。三个分区父级地点交叉为零；输入和目标+有效区域
在旋转、镜像后完全重复的跨分区地图为零；已知观测与目标无冲突。

选择清单先保存，再打开图片；目标图读取前固定所有查询。先按输入选择已知可通行起点和
未知有效区域内的目标点，**真实目标落在障碍上的查询仍记为负例**。不根据标签补选图片或查询。
训练/选优/验证分别有1327/340/515个查询，其中415/116/186个目标为障碍。
100个训练地点中2个没有合格查询，仍参与地图学习，不被悄悄删除；选优和验证地点均有查询。

距离40/80/120格，机器人半径0/10/20格，四邻接与离散圆盘。
本轮只声称格网几何，发布元数据0.01不能作为已独立验证的物理米制依据。
输入依赖数据集提供的有效区域，不宣称完整的纯传感器部署。

## 共同训练预算

| 项目 | 固定设置 |
|---|---|
| 训练模型 | ConPath、独立单元模型、确定性补全网络 |
| 重复 | 20260910、20260911、20260912；全部从头初始化 |
| 骨干 | 相同 F=16 的小型 U-Net；随机模型 latent=4 |
| 批量 | 每更新4个观测，微批量2，按数量累积梯度 |
| 优化器 | AdamW，学习率0.0003，权重衰减0.0001，梯度裁剪5 |
| 预算 | 最多24轮/600更新，至少12轮，之后选优连续6轮不改善可停止 |
| 随机模型损失 | 地图NLL + 0.1变差函数 + 2事件Brier无偏估计 |
| 确定性损失 | 未知有效区域的地图二元NLL |
| 训练采样 | K=4；每地点按输入编号哈希固定最多8个查询 |
| 检查点选优 | 25个独立选优地点的平均事件Brier最低；1e-5内同分时用地图NLL |
| 选优采样 | K=16；每地点随机流逐轮重置，不改变训练随机流 |
| 最终开发评估 | 九次训练全部结束后才开始模型验证评分；K=32及其前4张共同小预算表 |

三个损失按地点平均。无查询观测仍有地图损失。训练中的256步传播保留作梯度近似，
硬事件用无步数截断的精确连通性替换；记录两者差异。预测分数只来自真实二值世界，
不报告软传播值，也不从K张图中挑最好的一张。

训练和选优过程不读取验证缓存；准备阶段已为固定质量检查读取全部165个开发数据包的标签，
因此“训练结束后才做验证评分”不等于“此前未读取验证图片”。没有新增读取物理test数据包。

## 统计与后续判断

保留未知全可通行、全阻挡、最近观测和仅训练集拟合的半径先验。
报告按地点平均的通路Brier、30%覆盖率误判、未知区域IoU及K>1的MES。
风险按并列分数比例分摊，不能利用标签挑选相同分数中的安全查询。
配对区间按来源分层重采样父级地点；三个训练种子分别报告，不能把所有事件当独立样本。

实现检查通过后，若ConPath平均K32 Brier低于独立、确定性和全可通行规则，且30%覆盖风险
不劣于确定性网络，则进入一个预先固定预算的改进试验；这只是开发筛查，不等于显著性或论文领先。
若筛查未过，保留负结果并分析输入、查询及训练是否充分。不得为了通过而反复换验证集或删负例。
本轮最多600更新属于小预算，不保证所有模型已充分收敛；扩大规模和强外部共同评估仍是正式论文的缺口。
模型改进再使用本轮验证结果后，须明确其成为反复开发的数据，最终测试另行冻结。

## 可恢复执行

数据和源文件哈希在 `results/parent_group_pilot_v1/data/seal.json`、各次训练的 `recipe.json` 中。
完整世界、事件和原始统计另存独立目录。准备脚本拒绝覆盖冻结数据。

```bash
PYTHONPATH=src /home/hairo/miniconda3/bin/python3.13 scripts/run_parent_group_pilot.py --workers 2
```

每轮保存latest/best检查点、优化器和随机数状态。暂停只作用于本队列及自有子进程；
创建 `results/parent_group_pilot_v1/STOP` 会停止继续提交作业，恢复前移除该标记。
恢复从完整轮次边界重放，不把未提交的半轮更新算作已完成。
