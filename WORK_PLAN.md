# ConPath 当前工作计划

## 当前执行：基线完成，两种子改进训练中（2026-09-09）

九次父级地点隔离基线及结果/图片独立核验已完成；ConPath Brier 0.10380、独立对照0.11201、确定性网络0.11152，四项开发筛查通过，但相对确定性网络的配对区间仍跨零。新增10组本轮真实图片，首页共40组。详见[PARENT_PILOT_RESULTS_ZH.md](PARENT_PILOT_RESULTS_ZH.md)。

固定连续类别采样候选已启动20260910/20260911两个种子，最多同时2个进程，本项目实测显存53.1GiB，目标约60GiB；其它程序另计。监督器记录在`results/coherent_parent_pilot_v1/supervisor_process.json`，状态在同目录`status.json`。先检查状态，不重复启动；每个训练器28GiB分配器上限。完整配方、停止规则及同两个种子比较见[COHERENT_PILOT_ZH.md](COHERENT_PILOT_ZH.md)。

下一项：两个候选完成后自动统一评分，核验结果，再更新论文/网页；失败保留负结果，不自动扩大训练。原基线不再训练，所有最终测试仍锁定；本轮没有新增物理test读取。以下旧的“当前/下一项”按日期视为历史。


**读取范围更正：旧训练／验证包含原发布test中的8／5条观测，本轮回放也读取了这5条验证观测。更早的512观测查询审计检查过53条物理test观测（含32条ScanNet++）。因此撤回“物理test从未读取”的说法；旧 `test_evaluated=false` 等标志不能证明物理测试未触碰。详见[读取范围更正记录](site/data/flatlands_read_scope_erratum.json)。已停止进一步读取物理test图片，最终留出集需要审核全部历史访问并排除已检查的父级地点。**

更新：2026-09-09（America/New_York）。本文件记录当前可执行计划；
`ROADMAP_ZH.md` 保留完整研究路线，`CONTINUATION.md` 保留历史过程，
`RECOVERY_STATE.json` 记录恢复命令与文件完整性。

**2026-09-09 更正：旧 FlatLands 队列仅隔离子场景 ID，27/160个验证观测与训练共享建筑／地点。旧数值保留为队列诊断，不能证明新建筑泛化；旧子场景配对区间也不是独立建筑总体区间。数据隔离门槛重新打开。当前统一K=4对照、90条论文公开成绩、分组修正和历史帧分析见 [最新中文评估](BASELINE_REVIEW_ZH.md)。**

**当前状态：用户已明确恢复，要求同时最多两个训练进程、本项目显存约60GB。剩余随机种子20260911/20260912按队列继续；单进程PyTorch分配器上限28GiB，实测双进程约53GiB（不含其它程序）。原训练代码、数据和超参数保持冻结；全部完成后统一评分。**

用户最新偏好：优先引用可比较的论文公开成绩，其次复用作者预测/权重，最后才做最小必要的同协议适配训练；不无条件启动所有外部方法的长训练。外部对比仍需完成，不能用内部消融替代。

## 扩展效果图与连续实验（2026-09-09，最新）

- 新增20组室内（5个来源各4组）和8组室外（2个验证场景各4帧），共152张新增面板；加上历史2组，首页共30组。
- 新案例在推理前固定编号/时间；不按结果筛选，不作best-of-K。32张实际世界保存在`results/model_gallery_20260909_v1`，网页显示第1张及同批采样的概率。
- 新室内图排除旧训练父级地点，但仍来自旧选优验证队列；室外采用单独训练检查点。均为开发诊断，非最终测试。此次没有新增读取物理test图片。
- 新基线[固定协议](PARENT_GROUP_PILOT_ZH.md)已冻结，数据检查通过并开始九次训练。使用物理train内的父级地点三分区，从头训练；保留输入选择但真实目标落在障碍上的自然负例，避免目标标签筛选造成过于乐观的评估。
- 用户授权：图集后直接做小规模基线，验证好后直接改进。完整共同协议、训练和结果须另建版本，保留旧证据。

## 先前网站阅读入口与建议（2026-09-09，以下为历史）

首页已改成“模型效果 → 一张结果表 → 下一步”，研究细节集中到 `site/research.html`。
直接效果图入口是 `/#effects`，两个已训练模型的既有案例可切换实际补全/概率图；原始地图像素没有修改。
本轮没有新训练或重新推理；复用已发布图片，并校验现有资产哈希。旧数据隔离与访问问题没有因网页重排而解决。

建议下一项交付一轮**干净小规模基线验证**：先核定未触碰最终测试、父级地点划分、输入质量与盲查询，
然后在同一固定子集从头训练ConPath、独立模型、确定性网络，并保留无训练规则，检查原优势是否成立。
该轮须固定三次重复和共同评估预算；不把仅有两个示例或未经充分训练的试跑当成正式论文结果。
在此基础上再决定扩大规模及必要外部共同评估；室外先修观测与几何融合，再检验注意力架构。

## 目标与当前判断

形成可复现的“空间相关地图后验改善车辆尺寸条件路径概率”论文证据。
当前 P0 已通过；FlatLands K=128 三种子在旧队列有正向诊断结果，但建筑隔离不通过；
UnScenes3D 仅两个验证场景，相关模型和独立对照几乎一致。
工程审计通过不等于科研假设通过，当前不能宣称正式测试或跨域成功。

2026-09-02 的 13 小时计划是历史执行窗口，相关审计已经完成。
本次按交付物和验收条件推进，不重复已通过的检查来填满时间。

## 文献核查后的执行顺序（以下旧优先级受本轮修订约束）

2026-09-07 已阅读八篇相关论文并核查官方代码。当前外部强基线比较是主实验缺口，
不能用继续增加内部消融替代。具体方案见 [EXPERIMENT_DESIGN_ZH.md](EXPERIMENT_DESIGN_ZH.md)，
论文如何做比较的证据见 [LITERATURE_REVIEW_ZH.md](LITERATURE_REVIEW_ZH.md)。
优先 FlatLands 的 LaMa/集成与条件生成对照，以及 CogniPlan 原生地图上的生成模块；
KITTI-360 暂不列为必跑，等待明确三维任务需求。用户已明确确认此方案，并于21:42 EDT恢复既有消融。
恢复后的六组消融已全部完成；具体外部训练参数仍需在数据审计和测速阶段冻结。

| 阶段 | 当前状态 | 完成条件 |
|---|---|---|
| 1. 文献与实验设计 | 已完成八篇正文、官方代码入口和中文方案；进一步核清流匹配专属MSE损失与LaMa损失权重 | 明确外部方法、同骨干控制与消融的不同作用 |
| 2. 外部方法与数据适配 | 新父级候选215,289条元数据，三分区父级交叉为零；质量/尺度/最终测试未过门槛。三种外部模块均接通；LaMa/流匹配各完成批量2及实际有效批量64测速。FlatLands物理尺度仍未独立核实，正式数据规模和质量检查待完成 | 保留核心机制，先完成原生质量检查；方案中的数据与参数门槛通过后再安排训练 |
| 3. 正式主对比 | 现有K=4本地六种方法评估和论文90条参考已整理；建筑隔离失败，正式共同评估仍缺 | 同数据/查询/几何评估，K=4共同输出表与实际成本曲线，独立训练重复及明确的实现身份 |
| 4. 采样与消融 | 当前有界版本完成：no_event / no_global 各三次训练、精确评估与场景配对统计通过 | 若扩大数据，完整模型与消融另开一致的新版本 |
| 5. 第二域与泛化 | UnScenes3D 观测下界分析完成；拟增加 CogniPlan 原生地图比较，KTH 为后续补充 | 不把不同预训练来源当公平零样本比较，不用测试选择观测规则 |
| 6. 算法与论文冻结 | 现有数值与图已完成；外部比较、尺度核查和算子效率证据未完成 | 主表、图、方法、局限与有效报告一致；不预先承诺外部方法优势 |
| 7. 正式测试与投稿 | 尚未启动 | 方法、数据、预算与分析规则冻结后决定正式测试 |
| 网站与版本 | 本轮新增统一K=4图表、公开参考表与建筑重叠提示；旧图片保留 | 本轮凭据在 results/baseline_review_publication_20260909/；旧外部检查和消融收据保留 |

## 证据使用规则

- 当前主结果来自 `*support_clamped_v1*` clean training；UnScenes3D 均值图评估和定性图使用最终支撑投影修复后的 `v2`。修正前训练和旧 checkpoint 的事后修正不能充当 clean 消融。
- FlatLands 旧 provenance split 只有子场景隔离，建筑隔离失败；新候选位于 `flatlands_parent_groups_v2`，尚未成为正式训练协议；物理 test 与 UnScenes3D `location_6` 继续锁定。
- 新实验使用父级建筑／地点配对统计；旧子场景统计降为条件诊断；不能把同一场景内的 4,224 个事件当作独立样本。
- false-safe 必须同时报告覆盖率；阈值 0.8 的改善不能替代等覆盖率比较。
- 正例和失败图片均保留来源、查询、随机种子、checkpoint 哈希和按标签选例的披露。
- 不更改冻结的 split/query 或覆盖历史结果来制造优势。未达到研究门槛时，论文保持 working draft。

## 已发现并需避免的续接问题

- `RECOVERY_STATE.json` 曾把 9 月 4 日已完成的最终审计继续列为待办。
- 旧恢复命令指向修正前 K=128 目录；续接应使用 clean-support 目录。
- 当前 `.venv` 是 Python 3.11.15，clean checkpoints 与本轮外部模块检查使用 Python 3.13；
  本机检查点回放固定使用 `/home/hairo/miniconda3/bin/python3.13`。
- 路线图中的“保留消融”不代表这些结果仍有效；旧校准和均值图必须明确标记为历史记录。

## 已有论文分析与恢复状态

- 九方法主表和五组 SVG/PDF 图由 [PAPER_EVIDENCE.md](PAPER_EVIDENCE.md) 的命令生成。
- 固定经验边际后打散空间结构，Brier 从 0.06749 升至 0.16139；联合结构的作用有直接验证依据。
- 同检查点均值图 Brier 为 0.06957，与随机事件估计差距很小，三个逐 seed 区间均包含零。
- 等 30% 覆盖率风险 3.60% vs 3.90%，三个逐 seed 区间也都包含零；不宣称稳定安全收益。
- UnScenes3D 观测约束造成验证 Brier 下界 0.47574，只有 15.66% 事件可由未知区域补全改变；
  单纯继续当前 adapter 的训练无法消除这些固定错误。
- 均值图/绘图后处理曾重新打开无效区域，已修复并保留 v1 历史。v2 六组验证 Brier 为
  0.51142 vs 0.51130，27,522 条预测均满足上下界；训练权重和隐藏区域地图指标完全复现。

同一 FlatLands clean-support 合同下的 no-event/no-global 三 seed 对照已完成：
固定 F=16、训练 K=8、验证 K=128/chunk=8、40 epoch 上限与 patience=8；
仅分别改 `reachability_weight=0` 或 `disable_global_factors=true`，每组独立目录，保留完整模型参考。
固定边际干预是评估时干预，不能替代这两项训练消融。

新结果：完整模型 Brier **0.06749 ± 0.00936**，无事件损失 **0.20425 ± 0.00322**，
无全局因子 **0.09499 ± 0.00157**。六个逐种子场景配对 Brier 区间均为正，支持这两项设计。
但六个等30%覆盖率风险区间都包含零，无全局因子的风险点估计在两次训练中反向；不宣称稳定安全收益。
完整中文说明、来源/半径分层和固定案例见 [EVALUATION_SUMMARY_ZH.md](EVALUATION_SUMMARY_ZH.md)。

第二域后续先做 train-only 的观测似然/置信度模型审计，再冻结新协议；不能按当前两验证场景
选择观测阈值，也不能直接解锁 `location_6`。本轮 supervisor 和 worker 均已退出；没有注册 runtime goal。

## 2026-09-07 历史执行记录

网站已按用户提供的 Lightweight-3DGS 参考重做中文排版，采用四个主要阅读章节；旧版页面归档。
新增中文图例、起点/目标标记、图片放大、五组桌面/手机图表、12 个数据示例和 18 帧同步浏览。
选型分析见 [DATASET_CHOICE_ZH.md](DATASET_CHOICE_ZH.md)：当前数据集不等于其他原论文使用的数据集，
自建对照不等于原方法复现。主结果数值保持不变；网页重做时的检查记录在 `results/site_redesign_20260907/`。

训练与分析规则已冻结在 [CLEAN_ABLATION_PLAN.md](CLEAN_ABLATION_PLAN.md)，实现提交为 `e6f0910`。
01:50 EDT 按用户要求安全暂停：三组 no_event 均完成第 4 轮，三组 no_global 尚未启动；
调度器和训练进程已退出，实验显存已释放。恢复状态以
`results/paper_clean_ablation_matrix_v1/progress.json` 与 `pause_checkpoint_audit.json` 为准。
九份 latest/best/interrupted 检查点的模型、优化器、配置和随机状态恢复核验通过。
21:42 EDT 用户明确要求继续，已由完整第4轮的 `latest.pt` 恢复，重跑第5轮，
不使用包含半轮更新的 `interrupted.pt` 替换它。三个种子并行，另三组自动排队；
九份暂停检查点另行备份，确认原配置、优化器与随机状态保留。恢复凭据在 `results/training_resume_20260907/`。
六组全部完成后再审计、生成候选图并更新论文与网站。
新增恢复/配置保护测试后，93 项单元测试通过；原始 full-model CSV 的独立回放也通过。

用户已确认的论文交付清单在 [实验方案第8节](EXPERIMENT_DESIGN_ZH.md#8-论文实验交付清单用户已确认)。
外部主比较、三次独立重复、统一采样与成本评估、场景配对统计、失败图和最终测试均需完成；
不以内部消融替代外部比较，不把没有显著优势的结果省略。

CogniPlan 训练数据审计见 [EXTERNAL_DATA_AUDIT_ZH.md](EXTERNAL_DATA_AUDIT_ZH.md)：已观测单元与完整目标零冲突，
但补全器/规划器有3,000个同名不同图的 ID，必须区分资产命名空间；发布权重 batch=24 与仓库默认32不同。
这里只完成训练数据审计，未运行外部模型，也未读取测试图。

23:47 EDT 再次暂停的凭据在 `results/training_pause_20260908T034736Z/`。三组 no_event 的 latest/best
共六份检查点已核验并备份，保留12/9/17轮历史、优化器与随机状态；最佳模型分别在4/1/9轮。
三组都已达到 patience=8，后续恢复仅重新执行未保存进度的精确评估，不再增加训练轮次。
未完成的评估没有逐场景检查点；该阶段的已耗时间不能在恢复时接续。

23:59 EDT已按用户的新指令恢复；凭据在 `results/training_resume_20260908_finalization/`。
三组均以 `--finalize-dir` 启动，保留原来的12/9/17轮历史和最佳模型，重新执行精确评估；
另外三组由同一个调度器自动接续，不需要再次询问是否继续。
