# 空间连续采样：两种子小规模改进 v1

2026-09-09。原基线九次训练已完成，7,920张世界地图、27,810条预算内事件预测及汇总通过独立核验。
ConPath的三种子Brier为0.10380，独立对照0.11201，确定性网络0.11152，全可通行规则0.18048。
四项预设开发筛查通过；相对确定性网络的父级配对区间仍跨零，不能宣称全面领先。

本轮根据用户“验证好了直接开始改进模型”的授权开展，遵守最新的两个任务、约60GB显存限制。
只运行20260910和20260911两个种子，并与原基线中**相同两个种子**比较。不得拿两种子新结果与
原三种子均值拼成优势表，也不自动增加第三种子或开展大规模搜索。

## 唯一模型变化与假设

旧解码器已有全局因子和局部高斯场，最后还对每个格子独立抽取类别噪声；真实图中可以看到局部零碎点。
固定尝试让最后这层噪声在空间上相关，检验能否改善连通结构与通路概率。它是待检验的假设，
不能仅凭视觉推断为主要误差原因。现有确定性均值图诊断Brier为0.11410，反而劣于原随机模型，
因此本轮仍保留随机世界并估计路径概率。

新增解码器从每个批项、世界和类别分别生成新的独立白高斯场，使用9×9、σ=3的固定高斯核，
按每个位置实际核权重平方和归一化方差，再通过标准正态CDF与Gumbel逆CDF生成空间相关类别噪声。
类别之间和不同世界之间保持独立，不能把同一噪声复制到K张地图。连续理想分布下逐格类别边际保持
softmax；实现沿用1e-6截断，因此只在浮点与尾部截断误差内成立，不声称完整地图的因子化似然。
正向采样噪声尺度固定1、反向温度0.7；固定边界与已知观测投影。

核宽度不调参；1×1只用于CPU合成对照，不启动另一项训练。没有增加学习参数，共120,108个参数。
替换解码器时复制原始初始化并恢复全局随机状态；两个种子的全部初始参数逐项相同。
候选额外消耗随机数、卷积和CDF计算，不能声称算力相同或逐世界随机流一致。

## 共同预算、选优与判断

沿用[原固定协议](PARENT_GROUP_PILOT_ZH.md)的100/25/40个父级地点、数据及查询、损失、有效批量4、
微批量2、AdamW学习率0.0003等全部训练参数；最多24轮、600更新，选优K16、评估K32及前4张。
每个种子从头训练，不加载原已训练权重，不提前读取本轮模型的验证评分。
原事件损失对零查询微批的聚合限制原样保留，使此次只检验采样机制；正式配方修正需另建版本。
新latest额外保存所选权重，恢复时重建best，避免两个检查点文件非事务写入导致的恢复不一致；不改变选优规则。

两个候选均结束后统一评分。开发筛查固定为：相对同两个种子的原ConPath，平均K32 Brier更低，
且30%覆盖率误判不更高。分别报告种子、K4/K32、地图指标、按来源分层的父级配对区间和失败结果。
这只是有限开发筛查；区间仅条件于两个训练种子的平均值，不能替代更多独立训练与最终测试。

这40个地点现已用于选择改进方向，明确标记为反复开发验证；不改数据、不删失败例、不解锁物理test。
通过后再考虑扩大训练规模及强外部共同协议对照；未通过则保留负结果并停止本候选的自动扩训。

## 执行与恢复

原始文件位于`results/coherent_parent_pilot_v1/`。protocol记录训练开始前的源码、数据与基线核验哈希。
同时最多2个训练进程，每个PyTorch分配器上限28GiB，项目显存目标约60GiB，其它程序另计。

```bash
/home/hairo/miniconda3/bin/python3.13 scripts/run_coherent_parent_pilot.py
```

`STOP`标记停止队列；只有用户恢复授权后移除。监督器持有独占锁，禁止重复提交。
训练、结果及来源另存，不覆盖原基线。论文和网页只展示已完成且通过核验的结果。
