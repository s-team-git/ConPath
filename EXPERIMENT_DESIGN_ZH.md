# ConPath 对比实验方案

制定日期：2026-09-07。依据 [八篇论文的对比方式与代码核查](LITERATURE_REVIEW_ZH.md)。
状态：**实验方案已经用户确认；截至2026-09-08 04:56 EDT，原有六组训练消融、精确评估和配对统计已完成。外部主比较仍待运行，正式测试继续封存。**
本方案确立研究优先级，不覆盖既有实验、查询或检查点。具体训练规模与运行参数须在小样本测速后冻结。

方案确认长期有效：后续按下列优先级完成实验并写入论文。当前没有暂停要求；已完成的旧矩阵不重复启动。
确认时的方案原文与哈希保存在 `results/training_resume_20260907/accepted_experiment_design.md`
和 `authorization.json`，机器续接记录见 `RECOVERY_STATE.json`。
已冻结的消融配置保持不变；外部方法尚未确定的具体参数不能当作已冻结配方。

## 1. 要证明什么

主问题：给定同一张部分观测地图、起点、目标和圆盘半径，ConPath 能否以有竞争力的计算成本，
更准确地估计“完整地图中是否存在通路”的概率？

当前证据表明空间联合结构值得研究，但均值地图结果已很接近，等覆盖率风险也没有稳定改善。
正式实验必须保留这些强对照；结果若仍接近，应限定论文贡献，不能靠弱化对手或只增加消融支撑结论。

## 2. 数据安排

| 数据 | 拟承担的作用 | 比较条件与启动前要求 |
|---|---|---|
| **FlatLands：主实验** | 真实室内地图；外部补全方法、路径概率与效率比较 | 使用场景隔离的统一划分。现有160个训练观测先用于工程检查，不能称为原论文完整规模。正式训练集合须预先确定，所有方法相同；若扩大，所有方法另开版本重训，不混入旧结果。 |
| **CogniPlan 原生模拟地图：第二组主比较** | 直接采用公开条件生成模块，与 ConPath 在它的原生输入上比较 | 核对补全器与规划器数据的母地图 ID、训练重叠和布局标签；从原始训练母地图划出验证集，测试地图不调参。所有新训方法使用同一批部分/完整地图。 |
| **KTH：后续泛化补充** | 真实建筑楼层图，检查模拟地图训练后的迁移 | 优先共同使用模拟地图训练后冻结的模型。已在 KTH 微调过的 MapEx 权重单独列为域内参考；不能混称同等零样本迁移。按建筑分组，先审计已有权重的训练重叠。 |
| UnScenes3D | 保留已有观测失配的负结果分析 | 先检查观测约束；当前不追加同一错误约束下的训练。 |
| KITTI-360 / SSCBench-KITTI360 | 有条件的三维传感器扩展 | 当前不列为必跑。只有论文明确增加三维输入或完整感知系统比较，且标签与输入能对齐时，才进入下一版计划。 |

FlatLands 的主定量实验本来就在 BEV 层比较，故保留它不妨碍比较外部方法。
关于分辨率、发布划分与论文描述的差异，先进行 train-only 的元数据和变换检查。
现有0/10/20格结果原样保留；尺度核实前，不据此扩展车辆物理尺寸结论。

## 3. 优先比较哪些方法

| 优先级 | 方法与论文来源 | 本项目的准确名称、使用方式 | 是否需要训练 |
|---|---|---|---|
| 必做 | **LaMa**，及 MapEx/FlatLands 使用的 LaMa 集成 | 保留 Fourier 卷积骨干，适配为 BEV 补全；单模型与4成员集成分别报告。它是公开方法的补全模块适配，不是完整 MapEx 探索器。 | FlatLands 主表需同数据训练或微调；公开权重仅用于原生检查/单独的迁移表。 |
| 必做 | **条件流匹配 + 交叉注意力（FM+XAttn）**，FlatLands 随机补全强对照 | 按文献的条件流匹配方程与粗尺度条件注意力实现，保留生成整图的机制；称“文献重实现”，实现差异可查。 | 需要。官方代码仍待发布；先解决损失、骨干与采样约定，不允许用明显失败的重实现作弱对手。 |
| 必做，第二数据组 | **CogniPlan 的条件生成补全模块**，CoRL 2025 | 复用官方 Generator 与布局条件，在其原生地图上做通路事件评估。原生4假设及后处理完整记录。 | 官方权重先做原生检查；正式重复实验按同一训练划分重训。 |
| 扩展 | **条件扩散（DDPM/DDIM）**，FlatLands 的另一随机生成族 | 与流匹配共享条件信息，单列去噪步数与计算量，避免结论只针对一种生成族。 | 需要；在前三项可运行后加入，不按结果是否有利决定是否展示。 |
| 备选 | **UPEN 的地图预测器/集成**，ICRA 2022 | 当需要额外地图先验基线时使用官方预测模块；它的候选路径启发式不直接当作事件概率。 | 需要适配/重训；优先级低于上述更近期且更贴近补全任务的方法。 |
| 保留 | 独立单元、确定性补全、同检查点均值地图、直接事件预测 | 强简单方法及我们自己的控制组；保持准确命名。主表中的学习方法须补齐独立重复，不能把一次训练写成三次。 | 已有有效结果能复用则复用；新数据/规模改变时统一重训。 |

PaSCo、S4C、SGN、SceneSense 继续作为相关工作与评估设计参考。当前不为凑近期论文名称，
强行把完整三维模型改成小型二维网络；它们不列入这一版必须训练的清单。

主比较覆盖三种不同竞争机制：强确定性地图、多个模型形成的集成、单模型生成多个完整世界。
此外保留直接预测通路事件的对照，检验是否有必要先生成地图。

## 4. 输入与预测必须怎样统一

输入统一为观测可通行、观测阻挡、未知三个状态，并明确提供同一有效范围。
某外部模型使用“部分地图 + 未知掩码”的编码时，只做信息等价的转换；不额外输入完整地图、
隐藏区域类别、测试场景类型或由答案选择的布局条件。所有训练辅助标签、外部预训练来源另列。

每个完整地图预测先恢复观测证据、封闭无效范围，再使用相同的圆盘收缩和四邻接连通判断。
对第 k 个预测地图得到事件 z_k，通路分数为这些事件的平均值。
确定性方法只有一个地图，原始通路输出为0或1；直接事件模型直接给出概率。
不能用“路径上最小单元格概率”“平均地图上的路径”替换整图事件平均后仍用同一名称。

CogniPlan 的4个输出来自预设布局条件，不是独立随机样本。原样保留这一机制，称为布局投票分数，
由 Brier/校准实验检验它是否可作为可靠概率。推理不得选择真实布局类型。
原生阈值及5格形态学处理作为方法的一部分记录；公共支持边界投影始终最后执行。
若研究统一后处理的影响，作为额外配对实验报告，不能悄悄替换原方法设置。

## 5. 主表、图和公平预算

### 主表A：固定输出数量的外部方法比较

第一张表统一随机/多假设方法为 **K=4**：ConPath、独立单元、4成员 LaMa 集成、FM+XAttn；
加入扩散时同样为4。确定性方法保留原生1个输出，不复制成4个不同世界。
第二数据组再加入 CogniPlan 原生4假设。所有方法使用同一套起终点与半径。
保留外部骨干的合理容量；只有明确研究机制的控制组才强制使用同一个 TinyBEV 骨干。

这是新比较表，不能把现有 K=128 的 ConPath 数字直接填进去。旧验证表继续归档保留。
MapEx 原生3成员设置若另行运行，须在原生设置或成本表中明确写3，不能冒充4成员。

### 主表B与成本曲线：实际可用的精度和代价

保留 ConPath 的 K=128 参考；对可增加采样数的生成器评估 K=4/16/32/128 中预算允许的点。
有限集成/条件向量不能靠重复同一输出伪造独立样本数。
所有被执行的点完整展示，未执行点标注未执行，超预算/显存点记录原因。

报告每观测的地图生成、后处理、通路判断和总延迟，以及峰值显存、全部模型参数量、实际网络调用量。
同一硬件、固定查询负载、预热后计时，并记录批量与 CUDA 同步方式。
比较延迟—Brier 曲线；共同预算点在测速后、看正式测试结果前冻结。
相同 K 不等于相同计算成本，求解器步数也不一定等于实际网络调用量。

### 指标与图像

| 层面 | 预定指标/图 | 解释 |
|---|---|---|
| 主任务 | 场景等权的路径 Brier；辅助 NLL、10箱 ECE | 预测概率是否准确、是否过度自信；不能用地图 IoU 代替。 |
| 查询接受风险 | 相同10%/30%/50%覆盖率的实际无路比例，30%为重点；整条风险曲线 | 不能只挑各方法不同覆盖率的阈值成绩。 |
| 地图质量 | 未知有效范围内的可通行 IoU、阻挡 IoU、单元格 Brier；多样本 Energy Score | 看事件差异是否仅来自基础地图质量；指标定义和空集合约定预先冻结。 |
| 稳健性 | 按来源、半径、观测比例分层，另列失败情形 | 分层规则不按模型输赢设定；不把重复观测当作独立场景。 |
| 可视化 | 相同观测、相同查询、相同样本序号；正例与失败例并列 | 标清观测/预测/参考、颜色和起终点。不用真实地图挑“最好的一次生成”来展示实际可用性能。 |

文献的 best-of-K 分数需要答案来选样本。若为对接原论文额外报告，只能放在明确标注为
“知道答案后的最好样本”的辅助表，不能作为通路概率主成绩。

增加一个统一的后校准对照：从训练母场景中单独留出校准部分，所有方法使用相同的单调概率映射流程；
原始与校准后结果成对报告。不得拿最终验证/测试标签拟合校准器。旧结果不因此回写。
校准母场景必须未被相应检查点用于训练；复用旧检查点时不能把它看过的训练样本事后称为留出集。
这能检验复杂生成模型是否优于“强均值地图 + 简单校准”。

## 6. 重复实验与消融

正式新训方法预定3次独立训练，按母场景/建筑聚合并做配对不确定性分析。
训练种子的标准差与场景重采样置信区间分别报告；不把成千上万条同场景查询当独立样本。
主量先分别计算每次训练的场景等权 Brier，再取三次均值，不先平均模型概率而无意间变成新集成。
配对场景重采样预定2000次；若固定三次训练求区间，明确它反映场景抽样不确定性，不能冒充训练种子总体区间。
集成成员与独立重复不是一回事：4成员集成做3次独立重复，原则上需要3套成员，不能把4个成员当4次实验。
若受限于公开单检查点，只报告一次运行的场景区间，并注明没有训练重复方差。

消融保留已冻结的 no-event/no-global 方案、均值地图、固定边际打散、采样数与尺度分析。
它们回答模块和机制是否有效，外部方法主表回答相对已有方法的价值，两者都需要。
原有界矩阵现已完成：no_event 在12/9/17轮停止，no_global 在12/18/18轮停止；六组完整验证和审计通过。
三种模型 Brier 为完整0.06749、无事件损失0.20425、无全局因子0.09499；六个Brier区间为正，等覆盖率风险区间仍都包含零。
详细证据见 [EVALUATION_SUMMARY_ZH.md](EVALUATION_SUMMARY_ZH.md)。一旦扩大训练集合，新规模的完整模型和消融须使用同一新协议。

## 7. 执行顺序与验收

| 顺序 | 交付物 | 验收条件 |
|---|---|---|
| 0，当前已完成 | 文献对照表、代码入口与本实验方案 | 八篇正文已核查；每个候选的任务、数据、实现身份和局限清楚。 |
| 1，下一步 | 外部方法适配清单与数据协议审计 | 核对 LaMa 输入/掩码，明确流匹配损失与网络配置，核对 CogniPlan 母地图划分与条件标签；先解决尺度差异。 |
| 2 | 原生检查与小规模资源测量 | 验证公开模块能产生合理输出；每个方法在固定训练样本上检查损失、观测保持、连通边界及单轮耗时，生成可核查的总预算。 |
| 3 | 冻结正式主比较配置 | 确定共同训练/校准/验证集合、外部预训练记录、各方法足够收敛的训练配方、三次重复、采样预算与所有指标。不能强迫不同模型都只训练40轮而使某一方欠拟合。 |
| 4 | 主比较、消融、效率与失败分析 | 完整展示结果和不确定性；外部方法质量检查不过就诊断，不把故障分数当最终基线。 |
| 5 | 冻结后的一次正式测试及论文更新 | 测试前完成方法与分析冻结。测试无优势同样报告；不得用测试挑模型、改半径或删场景。 |

步骤1已在既有消融续训期间推进。当前既有消融已完成；CogniPlan 已完成母地图及旋转/镜像去重划分，
32例/128图原生推理检查和重建/对抗各100次更新测速；正式外部训练尚未启动。
新母地图划分2,400训练/300校准/300验证，固定测速样本只来自训练部分。
原始训练图上的发布权重检查不构成留出验证，详见 [EXTERNAL_PROGRESS_ZH.md](EXTERNAL_PROGRESS_ZH.md)。
FlatLands的160份训练元数据确认裁剪形式，但论文另有缩放描述，物理尺度仍未独立验证；继续按格报告。
流匹配专属损失已在补充S5核实为masked MSE，不能用主文笼统BCE替代。LaMa/流匹配接入与正式配方仍待完成。
KTH/KITTI 数据尚未获取，所有最终测试保持隔离。
已完成的资产检查见 [EXTERNAL_DATA_AUDIT_ZH.md](EXTERNAL_DATA_AUDIT_ZH.md)：确认3,000张完整图、23,795张部分图，
记录灰度转换、跨资产同名不同图和发布配置差异；这些发现用于落实接口，不改变预先确定的评价指标。

步骤2的预算检查预定使用32个固定训练观测，先预热再记录100次更新与固定查询负载下的推理时间。
这只检查资源和数值行为，不用短训练的胜负决定保留哪个方法，也不要求生成模型在100步内收敛。
原生检查限定训练或预先隔离的验证数据；若原论文没有可对接的验证参考，不宣称复现其测试成绩。
总预算按实际每步成本、正式训练规模、各方法配方、集成成员数和独立重复数计算，不沿用旧模型的几小时估计。
论文的独立推理耗时与成本比较应在同一设备、无其他训练负载时测量并记录环境；
当前三组并行训练的轮耗时只用于排期，不混作单模型性能数字。

## 8. 论文实验交付清单（用户已确认）

下列必做项进入持续工作计划；完成必须有配置、预测与分析文件作为证据，不能仅凭一次训练退出标记完成。

- [ ] **数据与协议表**：母场景隔离、训练/校准/验证划分、尺度、查询规则、预训练来源与正式测试冻结记录。
- [ ] **FlatLands 外部主表**：LaMa、4成员集成、FM+XAttn，与 ConPath 和强简单方法使用共同输入和几何评估；新训方法3次独立重复。
- [ ] **CogniPlan 原生地图主表**：保留官方条件生成核心与4个原生假设，统一训练数据与查询，明确模块比较范围。
- [x] **机制与消融表（当前有界验证版本）**：完整模型、去事件损失、去全局因子、均值地图、固定边际打散均有有效证据；六组新训练已按原合同完成。数据扩展时仍须另开一致的新版本。
- [ ] **精度与成本图**：共同 K=4 输出表，以及实际 K、网络调用、耗时、峰值显存和参数量的成本曲线。
- [ ] **可靠性与统计表**：场景等权 Brier 为主指标，NLL、ECE、等覆盖率风险、训练重复标准差与2000次场景配对重采样区间分开报告。
- [ ] **图像与失效分析**：固定来源的成功和失败案例，中文图例、观测/预测/参考标签、半径与尺度说明；分来源、可见比例和半径检查。
- [ ] **冻结测试、论文与网站**：协议冻结后执行正式测试；把全部有效结果、无显著差异和局限写入论文，并同步可追溯表图与版本。

条件扩散为已列入方案的扩展；KTH 为泛化补充；KITTI-360 仍取决于论文是否增加三维系统主张。
这些优先级不按结果是否有利更改。科学结论可被反驳，验收目标是比较公平、记录完整、结果可复现。

## 9. 当前不承诺的结论

这一版首先检验地图与通路概率，不直接主张优于完整 CogniPlan/MapEx 导航系统。
若论文增加导航优势主张，另做固定传感器与规划设置下的闭环实验，报告成功率、碰撞、距离和耗时；
届时才把完整规划器作为对照，不能把静态连通性图当作行驶轨迹。

外部强基线尚未运行，不能预先保证 ConPath 会领先。若只在空间结构机制上有证据、
在强均值图或生成基线上没有稳定收益，应据此收窄论文主张或改进方法。
