# 外部对比实验：训练数据与接口审计

更新：2026-09-07（America/New_York）。执行依据是用户已确认的 [实验方案](EXPERIMENT_DESIGN_ZH.md)。
下文保留9月7日的训练资产审计。9月8日后续已完成母地图划分、原生推理与短训练测速，
见 [EXTERNAL_PROGRESS_ZH.md](EXTERNAL_PROGRESS_ZH.md)；正式对比仍未启动。

## 已核实的 CogniPlan 资产

从 [官方发布包](https://github.com/marmotlab/CogniPlan/releases/tag/paper_model_exploration)
取得生成器权重、补全器训练地图和规划器训练地图，三个压缩包 SHA-256 均与
[固定版本下载脚本](https://github.com/marmotlab/CogniPlan/blob/444fab8d5d3b8d2b004d83865088a9d707d2e3c8/dataset/download.sh)
一致。没有下载或读取 `maps_eval` / `maps_eval_inpaint`。

| 检查项 | 实际结果 | 对实验的影响 |
|---|---|---|
| 补全器完整训练地图 | 3,000张，room/tunnel/outdoor各1,000张 | 按母地图划分，不能按部分观测图片随机拆分 |
| 对应部分观测 | 23,795张；每张都能找到母地图，全部母地图均有观测 | 同一母地图的所有观测必须留在同一个集合 |
| 原图形状 | 全部250×250 | 官方网络再补边到256×256；评估时还原原始范围 |
| 完整目标几何重复 | 3,000个不同的规范化像素哈希，没有完全相同的目标图 | 仅排除了像素完全一致，不代表已排除旋转或近似布局重复 |
| 已观测位置与目标一致性 | 检查396,699,786个已观测单元，冲突为0 | 当前发布编码经正确转换后，观测保持约束成立 |
| 规划器训练地图 | 6,000张；与补全器有3,000个同名 ID，但同名地图内容全部不同，跨包完全相同的目标图为0 | ID 必须带资产命名空间，不能只凭 `room_0` 等文件名判为同一母地图或训练重叠 |
| 发布权重配置 | `gen_00500000.pt`；配置 batch size为24 | 仓库配置默认32，与发布权重配方不同；正式重训前必须明确采用哪份配置 |

完整图和部分图中的灰度值**含义不同**。按照
[官方转换函数](https://github.com/marmotlab/CogniPlan/blob/444fab8d5d3b8d2b004d83865088a9d707d2e3c8/mapinpaint/tools.py)：

| 灰度值 | 部分观测图 | 完整参考图 |
|---:|---|---|
| 1 | 已观测障碍 | 转换后的障碍编码 |
| 127 | 未知 | 原始障碍，转换为1 |
| 208 | 本次部分图中未出现 | 机器人起始区域，转换为255，即可通行 |
| 255 | 已观测可通行 | 可通行 |

因此不能使用同一套原始灰度判断直接读取两种图片；原始起始标记也不能当作额外障碍。
布局类别用于保留官方训练机制；推理仍使用预先固定的四个条件，不读取测试地图真实布局类别来选答案。

## 本次资产审计后的门槛（9月8日状态见链接报告）

1. 基于母地图和几何重复检查冻结统一训练/校准/验证划分，建立带资产命名空间的 ID。
   发布配置的训练数据指向原始训练集合；对这个集合再划出的子集做公开权重推理，只能叫训练数据上的原生检查。
   正式比较需在新共同划分上重训各方法，不能把公开权重的这些结果叫未见数据验证。
2. 验证官方生成器的输出、四条件、阈值、形态学处理和观测保持，再对接统一圆盘连通性评估。
3. 对模型进行预定的资源测量，并冻结重训配方。当前下载和数据审计不计为模型复现成功。
4. FlatLands 尺度差异、LaMa 掩码接口和 FM+XAttn 实现细节仍待解决。
   最终测试的数据来源与重叠检查留待冻结阶段，当前未完成这一检查。

## 复现与证据

```bash
/home/hairo/miniconda3/bin/python3.13 scripts/audit_cogniplan_training_assets.py
```

审计直接流式读取三个已校验压缩包，生成地图/观测清单与结果，既不解压测试集也不调用 GPU：

- `results/cogniplan_training_asset_audit_v1/report.json`：结果、资产哈希、源码版本与剩余门槛。
- 同目录 `full_maps.csv`、`partial_maps.csv`、`planner_train_maps.csv`：逐文件来源和像素/文件哈希。
- 同目录 `released_config.yaml`：从权重包保留的原始配置。
- `results/training_resume_20260907/cogniplan_downloads.json`：下载 URL、字节数与校验结果。

压缩包和逐文件清单保存在本地忽略目录，恢复记录登记它们的完整性；本页与审计脚本进入 Git。
